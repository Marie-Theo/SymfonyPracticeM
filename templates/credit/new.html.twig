{% extends 'base.html.twig' %}

{% block title %}{{'New Credit'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Create new Credit'|trans}}</h1>

    {{ include('credit/_form.html.twig') }}

    <a href="{{ path('app_credit_index') }}"><button class="btn btn-primary">{{'back to list'|trans}}</button></a>
{% endblock %}
