{% extends 'base.html.twig' %}

{% block title %}{{'Edit Credit'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Edit Credit'|trans}}</h1>

    {{ include('credit/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_credit_index') }}"><button class="btn btn-primary">{{'back to list'|trans}}</button></a>

    {{ include('credit/_delete_form.html.twig') }}
{% endblock %}
