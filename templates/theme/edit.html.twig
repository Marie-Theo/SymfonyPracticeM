{% extends 'base.html.twig' %}

{% block title %}{{'Edit Theme'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Edit Theme'|trans}}</h1>

    {{ include('theme/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_theme_index') }}"><button class="btn btn-primary">{{'back to list'|trans}}</button></a>

    {{ include('theme/_delete_form.html.twig') }}
{% endblock %}
