{% extends 'base.html.twig' %}

{% block title %}{{'Country index'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Country index'|trans}}</h1>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>{{'Id'|trans}}</th>
                <th>{{'Name'|trans}}</th>
                <th style='text-align: center;'>{{'actions'|trans}}</th>
            </tr>
        </thead>
        <tbody>
        {% for country in countries %}
            <tr>
                <td>{{ country.id }}</td>
                <td>{{ country.name }}</td>
                <td align='center'>
                    <form class="btn-group" method="post" action="{{ path('app_country_delete', {'id': country.id}) }}" onsubmit="return confirm('{{'Are you sure you want to delete this item?'|trans}}');">
                        <a href="{{ path('app_country_show', {'id': country.id}) }}" class="btn btn btn-outline-info bt-sm" title="{{ 'show'|trans }}">
                            <i class="fa-solid fa-pen"></i>
                        </a>
                        <a href="{{ path('app_country_edit', {'id': country.id}) }}" class="btn btn-outline-warning bt-sm" title="{{ 'edit'|trans }}">
                            <i class="fa-solid fa-eye"></i>
                        </a>
                        <button class="btn btn-outline-danger bt-sm" class="btn" title="{{'Delete'|trans}}">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ country.id) }}">
                            <i class="fa-sharp fa-solid fa-trash"></i>
                        </button>
                    </form>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">{{'no records found'|trans}}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_country_new') }}"><button class="btn btn-primary">{{'Create new'|trans}}</button></a>
    
{% endblock %}
